Add these before a store build:

- `icon.png` (1024x1024)
- `splash-icon.png`
- `adaptive-icon.png` (Android)

You can export from the web app icon at `public/icon.svg`, or run `npx expo prebuild` after adding assets.
